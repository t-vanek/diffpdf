# .NET build output
bin/
obj/
[Dd]ebug/
[Rr]elease/
artifacts/
*.user

# Rider / VS
.idea/
.vs/
*.suo

# OS
.DS_Store
Thumbs.db

# Local diff artifacts
diffpdf-artifacts/

# Serilog output
logs/

# Job storage (artifacts/reports/logs per scope)
storage/

# Sample / local instance data — not versioned
samples/

# Local release output from deploy/publish.ps1
publish/

# D3 Energy brand vector sources (.ai/.eps/.pdf/.svg originals) — ~2.8 MB of design
# binaries kept locally for regenerating the logo/icon; not needed by the build, which
# bundles only Assets/Brand/*.png|*.ico. Remove this line to version them if desired.
src/DiffPdf.DesktopUI/Assets/Brand/source/
