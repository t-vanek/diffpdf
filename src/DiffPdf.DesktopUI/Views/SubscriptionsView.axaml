<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:DiffPdf.DesktopUI.ViewModels"
             x:Class="DiffPdf.DesktopUI.Views.SubscriptionsView"
             x:DataType="vm:SubscriptionsViewModel">

  <DockPanel>
    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Spacing="8" Margin="0,0,0,8">
      <TextBlock Text="Subscriptions" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
      <Button Content="Obnovit" Command="{Binding RefreshCommand}"/>
      <Button Content="Nový" Command="{Binding NewCommand}"/>
      <Button Content="Editovat" Command="{Binding EditSelectedCommand}"/>
      <Button Content="Smazat" Command="{Binding DeleteCommand}"/>
      <TextBlock Text="{Binding Info}" Foreground="#9CDCFE" VerticalAlignment="Center"/>
      <TextBlock Text="{Binding Error}" Foreground="#C0392B" VerticalAlignment="Center"/>
    </StackPanel>

    <Grid ColumnDefinitions="*,360">
      <DataGrid Grid.Column="0" Margin="0,0,8,0" x:CompileBindings="False" ItemsSource="{Binding Subscriptions}"
                SelectedItem="{Binding Selected}" IsReadOnly="True" AutoGenerateColumns="False" CanUserResizeColumns="True">
        <DataGrid.Columns>
          <DataGridTextColumn Header="Channel" Binding="{Binding Channel}" Width="90"/>
          <DataGridTextColumn Header="Target" Binding="{Binding Target}" Width="*"/>
          <DataGridTextColumn Header="Scope" Binding="{Binding BranchKey}" Width="100"/>
          <DataGridTextColumn Header="Enabled" Binding="{Binding Enabled}" Width="80"/>
          <DataGridTextColumn Header="v" Binding="{Binding Version}" Width="50"/>
        </DataGrid.Columns>
      </DataGrid>

      <ScrollViewer Grid.Column="1">
        <StackPanel Spacing="6">
          <TextBlock Text="Odběr" FontWeight="Bold"/>
          <TextBlock Text="Channel"/>
          <ComboBox HorizontalAlignment="Stretch" ItemsSource="{Binding Channels}" SelectedItem="{Binding Channel}"/>
          <TextBox Text="{Binding Target}" PlaceholderText="Target (URL nebo e-mail)"/>
          <TextBlock Text="Události"/>
          <CheckBox Content="Completed" IsChecked="{Binding EventCompleted}"/>
          <CheckBox Content="GateViolated" IsChecked="{Binding EventGateViolated}"/>
          <CheckBox Content="Failed" IsChecked="{Binding EventFailed}"/>
          <TextBox Text="{Binding BranchKey}" PlaceholderText="BranchKey (volitelné)"/>
          <TextBox Text="{Binding InstanceKey}" PlaceholderText="InstanceKey (volitelné)"/>
          <CheckBox Content="Enabled" IsChecked="{Binding Enabled}"/>
          <Button Content="Uložit odběr" Command="{Binding SaveCommand}" HorizontalAlignment="Left"/>
        </StackPanel>
      </ScrollViewer>
    </Grid>
  </DockPanel>
</UserControl>
